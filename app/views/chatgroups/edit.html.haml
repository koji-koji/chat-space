.chat-group-form
  %h1 チャットグループ編集
  .chat-group-form__errors
    - if @chatgroup.errors.any?
      %ul
        -@chatgroup.errors.full_messages.each do |message|
          %li
            =message
  = render partial: 'form'
